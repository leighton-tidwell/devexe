import type { NextPage } from "next";
import { Button } from "@/components/index";

const Home: NextPage = () => {
  return (
    <Button type="button" onClick={() => {}}>
      Test
    </Button>
  );
};

export default Home;
